# coding: utf-8
# Cálculo de Campos de Futebol
total_campos = 0
for vezes in range(3):
	area = float(raw_input())
	campos = area / 4000
	print '%.2f' % campos
	total_campos += campos
total_campos = total_campos/3
print "%.2f" %total_campos
